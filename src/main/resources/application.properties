spring.application.name=AI-Assisted-Patient-Appointment-Helper

# Default profile is H2 (embedded DB) for easy setup
# To use MySQL with Docker, set: spring.profiles.active=mysql
spring.profiles.active=h2